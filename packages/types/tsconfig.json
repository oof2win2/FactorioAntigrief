{
	"extends": "../../tsconfig.json",
	"include": [
		"src/**/*",
	],
	"compilerOptions": {
		"outDir": "dist/",
		"target": "es6", // this is the lowest possible so that it's usable for everyone
		"composite": true,
		"typeRoots": ["./node_modules/@types", "./src/types"],
	},
}